BACKEND_PORT=8000
FRONTEND_PORT=5173

# Storage (dev local: backend/storage/cache e backend/storage/favorites)
# STORAGE_DIR=/caminho/storage
# CACHE_DIR=/caminho/storage/cache
# FAVORITES_DIR=/caminho/storage/favorites

# SQLite (padrão: backend/data/sources.db — Docker: /app/data/sources.db)
# DATA_DIR=/caminho/data
# DATABASE_URL=sqlite:///caminho/sources.db

# GALLERY_DL_BROWSER=vivaldi
